info:
   component: user-api-service
   version: 1.0.0
server:
   port: 8080

management:
   endpoints:
      web:
         exposure:
            include: prometheus,health,loggers

spring:
   profiles:
      active: dev